SERVER_PORT=8080
REGISTRY_TYPE=memory # memory | redis
# Redis settings (only used if REGISTRY_TYPE=redis)
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0
DEFAULT_TTL=0

DEFAULT_FUNC_CONCURRENCY=4 #workers per function
MAX_NODE_CONCURRENCY=0 #0 = disabled
QUEUE_SIZE_PER_FUNCTION=128 #in-flight waiting jobs per function
INVOKE_TIMEOUT_MS=5000 #server-side wait timeout